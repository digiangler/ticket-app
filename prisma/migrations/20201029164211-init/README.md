# Migration `20201029164211-init`

This migration has been generated by Digi-Angler at 10/30/2020, 1:42:11 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User" (
"id" SERIAL,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"authId" text   NOT NULL ,
"handle" text   NOT NULL ,
"name" text   NOT NULL ,
"picture" text   NOT NULL ,
"email" text   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Ticket" (
"id" SERIAL,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"ticketNr" SERIAL,
"userId" integer   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "User.authId_unique" ON "public"."User"("authId")

CREATE UNIQUE INDEX "User.handle_unique" ON "public"."User"("handle")

CREATE UNIQUE INDEX "Ticket.userId_unique" ON "public"."Ticket"("userId")

ALTER TABLE "public"."Ticket" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201029164211-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,33 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id        Int      @default(autoincrement()) @id
+  createdAt DateTime @default(now())
+
+  authId  String @unique
+  handle  String @unique
+  name    String
+  picture String
+  email   String
+
+  ticket Ticket
+}
+
+model Ticket {
+  id        Int      @default(autoincrement()) @id
+  createdAt DateTime @default(now())
+
+  ticketNr Int  @default(autoincrement())
+  user     User @relation(fields: [userId], references: [id])
+  userId   Int  @unique
+}
```


